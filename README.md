# CMS Events Content

Events Content owns events, sessions, speakers, venues, calendar visibility,
registration-provider references, archive lifecycle, and schema.org Event
structured data. It is provider-neutral: registration providers are stored as
references and adapters remain outside the domain package. API, Filament, and
Livewire integrations are optional matching packages.
